node_modules/
app/dist/
release/
*.log
.DS_Store
# dados de runtime — o engine NÃO versiona tarefas nem config (vivem no vault).
# defaults/ é o template de seed (versionado); config/ só aparece em runtime no modo default-vault.
tasks/.*.tmp
tasks/
config/
